autoload -U colors && colors
autoload -Uz compinit && compinit
autoload -Uz vcs_info
zstyle ':vcs_info:*' formats "(%F{2}%b%f)"
setopt prompt_subst

# VARIABLES
NEWLINE=$'\n'

# FUNCTIONS
precmd() {
    vcs_info
}

# THE PROMPT
PROMPT='${NEWLINE}%~ ${vcs_info_msg_0_}${NEWLINE}$ '
# PATH
export PATH="/usr/local/sbin:$PATH"

# ALIASES
alias gc='git commit -m'
alias gca='git commit -am'
alias gstat='git status'
alias gadd='git add .'
alias gdiff='git diff'
alias gdiffc='git diff --cached'
alias gco='git checkout'
alias gb='git branch'
alias gcob='git checkout -b'
alias gpushom='git push origin master'
alias hlt='heroku logs --tail'
alias gpushhm='git push heroku master'
alias gpushmm='git push mirror master'
alias bupdate='brew update'
alias bdoc='brew doctor'
alias boutdated='brew outdated'
alias bupgradeall='brew upgrade --all'
alias cdp='cd ~/www/personal'
alias cdsc='cd ~/www/SoundCollective'
alias msc='mongo soundcollective'
alias cdwild='cd $HOME/www/php/Wild/wp-content/themes/Divi-child'
alias cddots='cd ~/bin/dotfiles'
